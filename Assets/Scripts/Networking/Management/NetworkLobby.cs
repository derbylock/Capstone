using UnityEngine;
using System.Collections;

public class NetworkLobby : MonoBehaviour {
	/*public bool playerServer = true;
	public string levelToLoad = "Match Level";

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if(Network.isServer) {
			if(playerServer && Network.connections.Length >= 1) { NetworkLevelLoader.LoadLevel(levelToLoad); }
			else if(Network.connections.Length >= 2) { NetworkLevelLoader.LoadLevel(levelToLoad); }
		}
	}*/
}